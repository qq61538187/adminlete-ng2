import { TextlengthPipe } from './textlength.pipe';

describe('TextlengthPipe', () => {
  it('create an instance', () => {
    const pipe = new TextlengthPipe();
    expect(pipe).toBeTruthy();
  });
});
